---
description: Show the active fable-pack task and gate status
---

Run `python3 "${CLAUDE_PLUGIN_ROOT}/adapters/claude-code/scripts/pack" task status`, then `python3 "${CLAUDE_PLUGIN_ROOT}/adapters/claude-code/scripts/pack" validate --gate all` and summarize which gates pass or fail and what is missing.
