# Counterfactual Probe Prompt

Given the base task, perturb exactly one input. Record where the decision should
change and what reusable rule the boundary implies.

Do not evaluate code quality. Evaluate task understanding, context selection,
risk boundary, non-goal handling, and verifier contract.
